#include <stdio.h>
/**
 * main - Prints numbers of base 10
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int i;

	for (i = 28; i < 38; i++);
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
